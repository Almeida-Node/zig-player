{
  "name": "laboratorio-da-zig",
  "private": true,
  "version": "0.1.0",
  "description": "Laboratório da Zig — jogo de descoberta com a Professora Zig para crianças de 6 a 9 anos.",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "arte": "node scripts/checar-arte.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.1",
    "vitest": "^5.0.1"
  },
  "dependencies": {
    "@fontsource/montserrat": "^5.3.0"
  }
}
